{
  "name": "ramp-widget-signature",
  "version": "1.0.0",
  "description": "Ramp widget URL generation with Ed25519 signature",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "generate-keys": "npm run build && node dist/generateKeys.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "ramp",
    "widget",
    "ed25519",
    "signature"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
